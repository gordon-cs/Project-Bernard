import { notInArr } from 'test-app/helpers/not-in-arr';
import { module, test } from 'qunit';

module('Unit | Helper | not in arr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notInArr([42]);
  assert.ok(result);
});
