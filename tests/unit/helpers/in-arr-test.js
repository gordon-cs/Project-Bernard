import { inArr } from 'test-app/helpers/in-arr';
import { module, test } from 'qunit';

module('Unit | Helper | in arr');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = inArr([42]);
  assert.ok(result);
});
