import isLeader from 'test-app/utils/is-leader';
import { module, test } from 'qunit';

module('Unit | Utility | is leader');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = isLeader();
  assert.ok(result);
});
