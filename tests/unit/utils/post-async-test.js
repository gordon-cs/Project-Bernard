import postAsync from 'gordon360/utils/post-async';
import { module, test } from 'qunit';

module('Unit | Utility | post async');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = postAsync();
  assert.ok(result);
});
