<div class="col-sm-12 header">
  <nav class="container vertical-center graybox app-title-container">
      <div class="user-info col-sm-3">
        {{#if session.isAuthenticated}}
          <div class="user-info-buttons dropdown">
            <h4 class="dropdown-toggle" id=id="dropdownMenu1" aria-haspopup="true" aria-expanded="true">
                        {{session.data.authenticated.token_data.name}}
            </h4>
            <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown1menu1">
              <li>
                {{#link-to "help"}} Help{{/link-to}}
              </li>
              <li>
                {{#link-to "profile"}}Profile
                  {{#if requestsRecieved}}
                    <span class="notification">
                                    {{requestsRecieved.length}}
                                </span>
                  {{/if}}
                {{/link-to}}
              </li>
              <li>
                {{#link-to "transcript"}}Transcript{{/link-to}}
              </li>
              {{#if isSomeAdmin}}
                <li>
                  {{#link-to "activities-status"}} Activities' Status{{/link-to}}
                </li>
              {{/if}}
              <li role="separator" class="divider"></li>
              <li><a href="#" {{action "logout"}}>Logout</a></li>
            </ul>
          </div>
          {{#if requestsRecieved}}
            <span class="notification">
                            {{requestsRecieved.length}}
                        </span>
          {{/if}}
        {{/if}}
      </div>
      {{#if session.isAuthenticated}}
        {{#link-to "index"}}
          <div id="logo-box-horizontal">
            <img src="images/gordon-logo-horizontal.svg" alt="Gordon College logo" />
          </div>
        {{/link-to}}
      {{else}}
        {{#link-to "index"}}
          <div id="logo-box">
            <img src="images/gordon-logo-vertical-white.svg" alt="Gordon College logo">
          </div>
        {{/link-to}}
      {{/if}}
    </nav>
</div>






{{#if session.isAuthenticated}}
<nav class="navbar navbar-inverse">
   {{#link-to "index"}}
      <img src="images/gordon-logo-mobile.png" id="logo-mobile" class="{{if session.isAuthenticated '' 'logo-mobile-center'}}" />
    {{/link-to}}
    <div class="container-fluid">
    {{#if session.isAuthenticated}}
      <div class="mobile-menu-icon">
        <button data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
       </button>
      </div>
{{/if}}


    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="myNavbar">
      <ul class="nav navbar-nav navbar-right">
        <li class="active"><a href="#">Home</a></li>
        <li class="dropdown">
          <a class="dropdown-toggle" data-toggle="dropdown" href="#">All Events <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li>{{#link-to "chapel-credits"  }}
              <center>Chapel </center>
            {{/link-to}}</li>
            <li>{{#link-to "all-events"}}
              <center>All Events </center>
            {{/link-to}}</li>
          </ul>
        </li>
        <li>{{#link-to "index" class="button uppercase"}}
            My Home
        {{/link-to}}</li>
        <li>{{#link-to "all-activities" class="button uppercase"}}
            Activities
        {{/link-to}}</li>

        <li>{{#link-to "about" class="button uppercase"}}
              About
        {{/link-to}}</li>
        <li>{{#link-to "help"}} Help{{/link-to}}</li>
        <li>{{#link-to "profile" class="button uppercase" }}
              Profile
            {{/link-to}}
            {{#if requestsRecieved}}
                <span class="notification">
                          {{requestsRecieved.length}}
                                </span>
                    {{/if}}</li>

      </ul>
    </div>
  </div>
</nav>
{{/if}}
<div id="main-page-container" class="col-sm-12">
  {{outlet}}
</div>
